name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  macos:
    name: macOS
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive
      - run: swift --version
      - name: Build
        run: swift build
      - name: Test
        run: swift test
      - name: Test (Archive trait)
        run: swift test --traits Archive

  ios:
    name: iOS
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive
      # Cross-compile only — iOS runs on devices/simulators; the API is identical
      # to macOS, which runs the test suite above.
      - name: Build (iOS)
        run: xcodebuild build -scheme GitKit -destination 'generic/platform=iOS' -skipMacroValidation

  linux:
    name: Linux
    runs-on: ubuntu-latest
    container:
      image: swift:6.2-jammy
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive
      - name: Install C dependencies
        # libgit2 links -lz (zlib); HTTPS uses OpenSSL via dlopen at runtime.
        # bz2/lzma/zstd back swift-archive's filters for the Archive-trait run.
        run: |
          apt-get update
          apt-get install -y zlib1g-dev libssl-dev pkg-config ca-certificates \
            libbz2-dev liblzma-dev libzstd-dev
      - run: swift --version
      - name: Build
        run: swift build
      - name: Test
        run: swift test
      - name: Test (Archive trait)
        run: swift test --traits Archive

  windows:
    name: Windows
    runs-on: windows-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive
      - name: Setup Swift
        uses: SwiftyLab/setup-swift@latest
        with:
          swift-version: "6.3.1"
      - run: swift --version
      - name: Build
        run: swift build
      - name: Test
        run: swift test

  android:
    name: Android
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive
      - name: Build & test (Android emulator)
        uses: skiptools/swift-android-action@v2
        with:
          # Must exist in BOTH the swift.org host-toolchain feed and the
          # swift-android-toolchain SDK feed; 6.1.1 is in both (6.2.3 is SDK-only).
          swift-version: "6.1.1"
          free-disk-space: true
